#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='simple_api',
      version='1.0',
      description='A Simple API',
      author='Laurie Lay',
      author_email='llay@ippon.fr',
      packages=find_packages(),
      )
